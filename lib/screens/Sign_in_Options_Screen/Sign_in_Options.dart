import 'package:flutter/material.dart';

class Sign_in_Options extends StatelessWidget{
  const Sign_in_Options(BuildContext context);
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(child: 
      Column(
        children: [
          Text("Shabeeb")
        ],
      )),
    );
  }
}